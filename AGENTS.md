# AGENTS.md — Whisper Project

> 📍 **Start at [`PROJECT_INDEX.md`](PROJECT_INDEX.md)** — a generated, tool-neutral map of this repo (overview, architecture, per-subsystem key files & gotchas) for fast, low-token onboarding by any AI agent or human.

Instructions for AI coding agents (Claude Code, Codex, Cursor, Copilot, …) and CI.

- Read `PROJECT_INDEX.md` first — it summarizes the whole repo so you don't have to scan it (saves tokens).
- Project-specific Claude guidance lives in `CLAUDE.md`.
- The index's bottom "Structure" block auto-refreshes; regenerate it any time (deterministic, no AI):
  `python tools/index_refresh.py .`
- To rebuild the semantic sections after large changes, run the `project-index` workflow/skill.
