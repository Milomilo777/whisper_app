# Runtime dependencies for the Whisper Project
# Tested on Python 3.11+ (developer is on 3.14)

# Core transcription
faster-whisper>=1.0.3

# HTTP for model download + (future) auto-update checks
requests>=2.31.0

# UI theme (Phase 1.1)
sv-ttk>=2.6.0

# OS-correct config / cache / log paths (Phase 1.2)
platformdirs>=4.0.0

# DOCX export (core.writers.docx_writer)
python-docx>=1.0

# Drag-and-drop into the Tk window (loaded lazily — falls back gracefully)
tkinterdnd2>=0.4.0

# Speaker diarization (sherpa-onnx + pyannote-3.0 ONNX + 3D-Speaker
# embedding). No HuggingFace token required. Lazy-imported by
# core.diarization, so the absence of the wheel disables the
# Diarization toggle in the UI but doesn't crash anything else.
sherpa-onnx>=1.10

# Embedded media player for the transcript viewer (Tools → Open
# transcript viewer). Needs VLC installed on the system to render
# the libvlc.dll backend; the viewer falls back to a "Open in
# system player" button when libvlc isn't found.
python-vlc>=3.0

# PDF export (core.writers.pdf_writer)
reportlab>=4.0

# Folder watcher (core.watcher) — auto-transcribe files dropped
# into a configured watched folder.
watchdog>=3.0

# System-tray icon (core.tray + minimise-to-tray)
pystray>=0.19
Pillow>=10.0

# Alternative backend: whisper.cpp via pywhispercpp. Quantised ggml
# models (~1.1 GB for large-v3-q5_0) run on weak CPUs that struggle
# with the float16 faster-whisper path. Optional — the user can keep
# the default faster_whisper backend with no install changes.
pywhispercpp>=1.4

# Word-level alignment refinement (DTW). Loads its own tiny Whisper
# checkpoint (~75 MB) for the alignment pass — kept out of the
# transcribe backend so faster-whisper users skip the dep when they
# don't enable stable-ts.
stable-ts>=2.17

# Optional: dark/light system-theme detection used when theme="system"
# (sv-ttk falls back to dark if missing). Uncomment if desired.
# darkdetect>=0.8.0

# Note: tkinter is part of Python's standard library on Windows.
# On Linux, install python3-tk via apt/dnf/pacman.
# On macOS, tkinter ships with the python.org build.

# Optional: GPU support
# Install one of these manually if you want CUDA acceleration:
#   pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
# Or rely on a system CUDA install — faster-whisper will pick it up.
# Torch is NOT required for inference, only for the current device-detection
# heuristic which will be replaced by ctranslate2's native detection
# (see ROADMAP 2.6).

# --- Phase 1 leftover (uncomment when implementing) ---
# sentry-sdk>=1.40.0     # ROADMAP 1.8 — opt-in crash reporting

# --- Phase 2 additions (uncomment when implementing) ---
# tkinterdnd2>=0.4.0     # ROADMAP 2.12 — drag-and-drop
# watchdog>=3.0.0        # ROADMAP 2.12 — folder watcher

# --- Optional features (uncomment per feature) ---
# whisperx>=3.1.0        # ROADMAP 5.1 — speaker diarization
# demucs>=4.0.1          # ROADMAP 5.2 — vocal separation
# sounddevice>=0.4.6     # ROADMAP 5.3 — live mic
# fastapi>=0.110.0       # ROADMAP 5.4 — REST server mode
# uvicorn>=0.27.0        # ROADMAP 5.4

# --- Dev / test (use `pip install -r requirements-dev.txt` instead) ---
# pytest>=8.0.0
# ruff>=0.4.0
# pyright>=1.1.350
