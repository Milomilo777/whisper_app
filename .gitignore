# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Virtual environments
.venv/
venv/
env/
ENV/
.env

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# Project-specific
# Bundled binaries — too large for git, document the download in README instead
bin/ffmpeg.exe
bin/ffprobe.exe
bin/yt-dlp.exe

# Local config that may contain absolute paths / personal cache locations
config.local.json
# Migration / corruption artifacts (Phase 1.2)
config.json.corrupt
config.json.migrated.bak

# Logs (when Phase 1 lands)
logs/
*.log

# Test artifact dirs from acceptance runs
test_output_*/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
.tox/

# Type checker caches
.mypy_cache/
.pyright/
.ruff_cache/

# OS junk
.AppleDouble
.LSOverride

# Claude Code worktrees and per-machine permission settings (local, ephemeral)
.claude/worktrees/
.claude/settings.local.json

# Local notes that should not be committed
New Text Document.txt
NOTES.local.md
TODO.local.md
